org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.caching=true

# Matched to Voxy's own gradle.properties (MCRcortex/voxy @ 0.2.19-beta)
minecraft_version=26.2
loader_version=0.19.3
loom_version=1.16-SNAPSHOT
fabric_api_version=0.152.2+26.2

# Modrinth version IDs, not version numbers -- the same version number exists
# for several Minecraft versions, so the number alone is ambiguous.
# voxy 0.2.18-beta for MC 26.2
voxy_version=zZX86mbc
sodium_version=mc26.2-0.9.2-alpha.3-fabric

mod_version=0.1.0
maven_group=me.bbahablast.voxystats
archives_base_name=voxystats
